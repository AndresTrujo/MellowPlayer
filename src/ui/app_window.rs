//! Ventana principal de la aplicación (skeleton).
//!
//! No contiene lógica GTK real aún; sólo la definición del tipo.

pub struct AppWindow {
    // TODO: referencia a widgets, state binding
}

impl AppWindow {
    pub fn new() -> Self { Self {} }
    pub fn show(&self) { /* TODO: montar UI */ }
}
