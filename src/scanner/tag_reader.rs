//! Lector de tags/metadata (skeleton).

pub struct TagReader;

impl TagReader {
    pub fn read(_path: &str) -> Option<()> {
        // TODO: leer tags (id3, vorbis, etc.) y normalizar
        None
    }
}
