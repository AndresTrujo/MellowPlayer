//! `AudioPlayer` - interfaz/skeleton del reproductor de audio.

pub struct AudioPlayer {
    // TODO: internal state, pipeline handle
}

impl AudioPlayer {
    pub fn new() -> Self { Self {} }
    pub fn play(&self, _uri: &str) { /* TODO */ }
    pub fn stop(&self) { /* TODO */ }
}
