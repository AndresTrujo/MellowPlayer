//! Pipeline de audio (skeleton).
//!
//! Aquí se definirá la construcción del pipeline (GStreamer) en el futuro.

pub struct AudioPipeline {}

impl AudioPipeline {
    pub fn new() -> Self { Self {} }
}
