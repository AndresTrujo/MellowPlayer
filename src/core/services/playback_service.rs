//! Servicio de reproducción - capa abstracta que usa el motor de audio.

pub struct PlaybackService {
    // TODO: dependencia a `audio` layer via traits/events
}

impl PlaybackService {
    pub fn new() -> Self {
        Self {}
    }
}
